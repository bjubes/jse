language: python
python:
  - "3.6"
  - "3.7"
  - "3.8"
  - "3.8-dev"  
  - "nightly"  
install:
  - pip install pipenv
  - pipenv install pytest pytest-cov coveralls
script:
  - pytest --cov=jse
after_success:
  - coveralls
notifications:
  email: false